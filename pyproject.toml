[project]
name = "testproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jin", email = "122090871@link.cuhk.edu.cn" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
testproject = "testproject:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
